# Neighborhood v221

Compact numbered timeline with one fixed rail. Optional time remains in the content column. Uses existing theme tokens only. Manual order and edit/status/move handlers remain unchanged.

Validation: validate:v2 passed before final CSS density adjustment. After the adjustment, shell rebuild, collections contract and button sizing passed. Keyboard header contract passed. Browser fixture checked at 320/390/430: no horizontal overflow, all rails x=16, header y=0 height=68, all rows above 44px. Add-place form, edit-neighborhood form, stop action menu and empty state opened successfully.

Screenshot and geometry use synthetic local preview data. Physical iPhone keyboard and authenticated production mutations were not exercised in this redesign check.
