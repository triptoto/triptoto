# Secrets must never be committed.
# SESSION_SECRET is stored with `wrangler secret put SESSION_SECRET`.
# Optional internal QA only:
# TRIPTO_DEMO_SECRET=local-shell-copy-of-DEMO_TOOLS_SECRET

BETA_RELEASE=beta-milestone-4
BETA_METRICS_ENABLED=true
OPS_ENABLED=false
# OPS_SECRET=generate-a-long-random-secret-before-enabling-ops
